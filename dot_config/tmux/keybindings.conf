bind Enter set -g status
bind G run "~/.config/tmux/open-github.sh"
bind g display-popup -d "#{pane_current_path}" -w 80% -h 80% -E "lazygit"
bind s display-popup -E "~/.config/tmux/session-manager.sh"
